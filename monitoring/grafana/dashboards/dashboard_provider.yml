apiVersion: 1
providers:
  - name: 'Agent Dashboards'
    orgId: 1
    folder: 'AI Agents'
    type: file
    disableDeletion: false
    editable: true
    options:
      # This MUST match the internal container path in your docker-compose
      path: /etc/grafana/provisioning/dashboards
